# 阶段 3：并行与聚合（Parallel + Merge）

## 学习目标

- 理解一个节点状态如何并行流向多个节点
- 理解 `Annotated + operator.add` 的 reducer 思路
- 学会把并行结果汇总成最终输出

## 运行

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```
